import RegisterForm from "../../components/Form/RegisterForm";

const Register = (props) => {
    return (
        <>
            <RegisterForm {...props}/>
        </>
    );
}

export default Register;